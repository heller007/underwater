experiment_id: e2_naive_enhance
stage: e2
description: "Apply T0–T4 at test time on frozen E1 raw detector (no retraining)"
depends_on: e1_baseline
actions: [T0, T1, T2, T3, T4]
held_out_site: Lokrum
seed: 0
splits: [val, test]
# Set on CLI: --weights /kaggle/working/runs/.../train/weights/best.pt
status: ready
